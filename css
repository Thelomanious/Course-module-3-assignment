/* General body styling */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

/* Navbar background color */
.navbar-default {
    background-color: #007BFF; /* blue background */
    border-color: #007BFF;
}

/* Remove brand text color change on hover */
.navbar-default .navbar-brand,
.navbar-default .navbar-brand:hover {
    color: white;
}

/* Page heading */
.page-heading {
    text-align: center; /* center the heading */
    margin: 40px 0;
}

/* Tall section styling */
.tall-section {
    background-color: #0000fF; /* distinguishable background */
    min-height: 1000px; /* tall enough to scroll */
    padding: 50px 15px;
}

/* Center paragraph inside tall section */
.tall-section p,
.tall-section h1{
    text-align: center;
}

/* Mobile menu full width and background */
.navbar-collapse {
    background-color: #0056b3; /* distinguishable background for dropdown */
}

.navbar-collapse .nav > li > a {
    color: white;
    text-align: center;
}

/* Optional: Hover effect for menu links */
.navbar-collapse .nav > li > a:hover {
    background-color: #004080;
